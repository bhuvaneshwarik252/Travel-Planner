Goal:
Build a complete Phase-1 backend CRUD application using FastAPI and SQLite.
Antigravity should generate the FULL project structure and ALL files automatically.
This phase is ONLY application development.
Do NOT include Docker, Kubernetes, Helm, or cloud deployment yet.

====================================================
PHASE 1 — BACKEND APPLICATION (FOUNDATION)
====================================================

Tech Stack:
- Language: Python
- Framework: FastAPI
- Server: Uvicorn
- ORM: SQLAlchemy
- Database: SQLite (single file DB)

APIs to Implement:
- POST   /users        → Create user
- GET    /users        → Get all users
- GET    /users/{id}   → Get user by ID
- PUT    /users/{id}   → Update user
- DELETE /users/{id}   → Delete user
- GET    /health       → Health check endpoint

Database Details:
- Database file: `db/crud_db.sqlite`
- Table: users
- Columns:
  - id (INT, primary key, auto increment)
  - name (VARCHAR, not null)
  - email (VARCHAR, unique, not null)
  - created_at (TIMESTAMP, default current timestamp)

====================================================
PROJECT STRUCTURE (AUTO-GENERATE ALL)
====================================================

crud-app/
├── src/
│   ├── main.py              # FastAPI app entry point
│   ├── database.py          # DB connection & session
│   ├── models.py            # SQLAlchemy models
│   ├── schemas.py           # Pydantic schemas
│   ├── crud.py              # CRUD operations
│   ├── routes.py            # API routes
│
├── config/
│   └── settings.py          # App settings (DB path, logging)
│
├── db/
│   └── crud_db.sqlite       # SQLite DB file
│
├── .env                     # Environment variables (optional)
├── requirements.txt
└── README.md

====================================================
APPLICATION REQUIREMENTS
====================================================

- Use SQLAlchemy connection pooling
- Proper DB session management
- Basic error handling (404 if user not found)
- Add basic logging
- Avoid hardcoded values
- Clean separation of concerns
- Production-ready code style
- Keep project Docker & Kubernetes friendly for later phases

====================================================
OUTPUT REQUIRED (VERY IMPORTANT)
====================================================

Antigravity must output:
1. Full folder structure
2. Full backend code for all files
3. SQLite DB initialization (if needed)
4. requirements.txt
5. README.md with:
   - Project overview
   - How to install dependencies
   - How to run the application
   - How to test APIs using Swagger (/docs)

====================================================
STRICT RULES
====================================================

- Do NOT generate Dockerfile
- Do NOT generate docker-compose
- Do NOT generate Kubernetes YAML
- Do NOT generate Helm charts
- Focus ONLY on Phase 1
- Use SQLite as the database
